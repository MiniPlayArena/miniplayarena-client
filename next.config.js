/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['upload.wikimedia.org', 'www.snakesandladdersplus.com']
  }
}

module.exports = nextConfig
